# Default-deny the build context.
# This prevents accidental leaks from future local files such as:
# - .git metadata
# - .codex scratch state
# - .env files
# - signing keys, release artifacts, logs, caches
*

# Files required to build the pipe binary.
!go.mod
!go.sum
!*.go

# Keep the Dockerfile itself in context.
!Dockerfile
